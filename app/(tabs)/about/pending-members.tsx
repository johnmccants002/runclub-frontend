import PendingMembersScreen from "@/components/screens/PendingMembersScreen";
import React from "react";

type Props = {};

const Page = (props: Props) => {
  return <PendingMembersScreen />;
};

export default Page;
